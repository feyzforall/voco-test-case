import 'package:flutter/material.dart';

class VocoDimensions {
  VocoDimensions._();

  static const double smallGap = 8.0;
  static const double mediumGap = 16.0;
  static const double largeGap = 24.0;

  static const BorderRadius circularRadius = BorderRadius.all(Radius.circular(80.0));

  static const EdgeInsets pagePadding = EdgeInsets.all(12.0);

  static const EdgeInsets pd8 = EdgeInsets.all(8.0);
  static const EdgeInsets pd12 = EdgeInsets.all(12.0);
  static const EdgeInsets pd16 = EdgeInsets.all(16.0);
}
